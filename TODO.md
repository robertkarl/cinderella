# ready
[ ] no scrollback buffer... it's taking over tui but I don't think that's optimal.

[ ] Don't truncate the tool output. I kinda hate that about claude.

[ ] Emit the thinking tokens.

[ ] should be self-aware enough to monitor swap.

[ ] needs to update the UI with progress more often. it's been cranking for.... a couple minutes with no output.


# doing
